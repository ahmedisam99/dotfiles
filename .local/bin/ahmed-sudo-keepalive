#!/bin/bash

sudo -v || exit 1

while true; do
  sudo -n true
  sleep 60
done 2>/dev/null &

_SUDO_KEEPALIVE_PID=$!

_sudo_keepalive_finish() {
  kill "$_SUDO_KEEPALIVE_PID" 2>/dev/null || true
}

trap _sudo_keepalive_finish EXIT
